version: 2.1
orbs:
  heroku: circleci/heroku@1.2.3
workflows:
  heroku_deploy:
    jobs:
      - test:
          filter:
            branches:
              only: master
      - build:
          requires: 
            - test
          filter:
            branches:
              only: master
      - heroku/deploy-via-git:
          requires:
            - build
          filter:
            branch:
              only: master